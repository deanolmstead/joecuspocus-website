# joecuspocus.com

## Icons

Use **Simple Icons** (simpleicons.org) for brand/social icons (Facebook, Instagram, etc.).
Lucide removed brand icons in recent versions — do not use `data-lucide` for brand logos.
Add them as inline SVGs using paths from Simple Icons.
